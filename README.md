# support-rag-agent

Customer-support RAG agent. Routes an inbound ticket between retrieval, escalation,
and auto-reply paths using MCP tool-calling, backed by hybrid Elasticsearch retrieval.

Work in progress.
